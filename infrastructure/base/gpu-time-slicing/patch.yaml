apiVersion: nvidia.com/v1
kind: ClusterPolicy
metadata:
  name: cluster-policy
  namespace: gpu-operator-resources
spec:
  devicePlugin:
    config:
      name: time-slicing-config
      default: any
