resources:
- json-exporter.yaml

configMapGenerator:
- name: json-exporter-config
  namespace: monitoring
  files:
  - config.yaml

generatorOptions:
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: disabled
